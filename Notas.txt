> Reporte se guarda en carpeta principal.
> Falta escribir el LEEME
> Agregar palabras por defecto con definiciones reales
> Adaptar las aperturas de archivos a linux 
	> En vez de usar:  "directorio/archivo.ext", 
	> usar: "directorio" + os.sep + "archivo.ext" 
		> Importar "from os import sep" y que solo quede como "directorio" + sep + "archivo.ext"

> Falta un reporte en caso de que la palabra no se encuentre en Wikcionario ni en Pattern (aunque en Pattern siempre la da como válida... así que supongo que por eso no se genera)

> Seguro nos van a decir que la forma de resolver está mal, porque el jugador primero debe marcar todas las palabras y luego se debe revisar si están bien o mal, pero bueno, ya lo arreglaremos para la próxima entrega
	> "Una vez que haya terminado de marcar las palabras, se podrá cliquear sobre un botón
	  que permitirá revisar y verificar si se resolvió correctamente o no, mostrando los errores
	  cometidos en caso de existir.
	  La forma de mostrar los errores queda a criterio de cada grupo"

